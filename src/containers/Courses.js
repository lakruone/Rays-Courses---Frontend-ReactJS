import React, {Component} from 'react';
import Wrap from '../hoc/wrap';

class Courses extends Component {

  render(){
    return(
      <Wrap>
        <div>Search courses</div>
        <div>Display corses</div>
      </Wrap>

    );
  }
}

export default Courses;
